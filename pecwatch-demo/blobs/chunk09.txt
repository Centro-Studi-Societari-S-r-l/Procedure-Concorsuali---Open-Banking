ito')">＋ Nuova regola</button></div><div class="grid cards">${r.map((x,i)=>`<div class="card item"><div class="row between"><h3>${x[0]}</h3><input type=checkbox class=switch ${x[3]?'checked':''}></div><p>${x[1]}</p><b>${x[2]}</b><div class=actions style="margin-top:10px"><button class="btn sm" onclick="toast('Regola testata sullo storico demo')">Testa</button><button class="btn sm" onclick="toast('Editor regola aperto')">Modifica</button></div></div>`).join('')}</div>`}
function settings(){return `<div class="grid settings">${[['Classificazione assistita AI','Propone priorità e sintesi; conferma umana per i casi incerti'],['Alert scadenze','Promemoria a 60, 30, 15, 7 e 1 giorno'],['Report giornaliero','Invio del riepilogo operativo alle 07:00'],['Archiviazione integrale','Conserva EML, ricevute, allegati e hash'],['Integrazione Telemaco','Acquisizione simulata; nessuna API reale collegata'],['Audit append-only','Registra modifiche, assegnazioni e conferme']].map((x,i)=>`<div class="card setting"><h3>${x[0]}</h3><p class=mut>${x[1]}</p><label>Attivo <input type=checkbox class=switch ${i!==4?'checked':''}></label><button class="btn sm" onclick="toast('Impostazione salvata nella demo')">Salva</button></div>`).join('')}</div>`}
function render(){title.textContent=titles[view];nav();let h=view==='queue'?queue():view==='review'?review():view==='deadlines'?deadlines():view==='anomalies'?anomalies():view==='mail'?`<div class=toolbar><div><h2>Archivio PEC</h2><span class=mut>Tutte le comunicazioni, incluse quelle informative e tecniche.</span></div></div>${queue(true)}`:view==='companies'?companyView():view==='boxes'?boxView():view==='reports'?reports():view==='activity'?activity():view==='users'?users():view==='rules'?rules():settings();content.innerHTML=h;let q=$('#q');if(q)q.oninput=e=>{let t=e.target.value.toLowerCase();$('#content tbody').innerHTML=pecs.filter(p=>(p.co+p.sub+p.from).toLowerCase().includes(t)).map(row).join('')}}
function openPec(id){let p=pecs.find(x=>x.id===id);drawerId=id;drawer.innerHTML=`<div class=dhead><b>Dettaglio attività</b><button class=btn onclick="closeDrawer()">✕</button></div><div class=dbody>${badge(p.p)}<h2>${p.co}</h2><span class=mut>${p.date} · ${p.from}</span><h3>${p.sub}</h3><div class=box><b>Sintesi automatica · confidenza ${p.conf}%</b><p>${p.summary}</p><b>Azione proposta</b><p class=red>${p.act}</p></div><div class=tabs><button class="tab on">Sintesi</button><button class=tab>Messaggio</button><button class=tab>Allegati (${p.files})</button><button class=tab>Audit</button></div><p><b>Stato:</b> ${badge(p.st)}<br><b>Assegnatario:</b> ${p.as||'non assegnato'}<br><b>Scadenza:</b> ${p.due||'non rilevata'}</p><div class=box>Nella versione reale questa scheda collega originale .eml, ricevute, allegati, hash e fascicolo documentale.</div></div><div class=dfoot><button class="btn primary" onclick="take(${p.id})">Prendi in carico</button><button class=btn onclick="confirmDue(${p.id})">Conferma scadenza</button>