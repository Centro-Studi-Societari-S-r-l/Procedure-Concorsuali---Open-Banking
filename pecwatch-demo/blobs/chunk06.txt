0,70)}…</span></td><td>${p.act}</td><td class="${p.due?'red':''}">${p.due||'—'}</td><td>${p.as||'<span class=mut>Non assegnata</span>'}</td><td>${badge(p.st)}</td></tr>`}
function table(data=pecs){return `<div class="card tablebox"><table class="table"><thead><tr><th>Priorità</th><th>Società</th><th>Data / mittente</th><th>Oggetto</th><th>Azione richiesta</th><th>Scadenza</th><th>Assegnatario</th><th>Stato</th></tr></thead><tbody>${data.map(row).join('')}</tbody></table></div>`}
function queue(all=false){let data=all?pecs:pecs.filter(p=>chip==='all'||chip==='mine'&&p.as==='Utente Demo'||chip==='free'&&!p.as||chip==='p1'&&p.p==='P1'||chip==='late'&&p.due&&p.due<'24/08/2026'||chip==='rev'&&p.st==='Da verificare');return `${all?'':`<div class="grid kpis"><div class="card kpi"><div class="ico red">⚑</div><div><b>3</b><small>Urgenti P1</small></div></div><div class="card kpi"><div class="ico amber">◷</div><div><b>3</b><small>Da verificare</small></div></div><div class="card kpi"><div class="ico blue">▣</div><div><b>4</b><small>Scadenze entro 30 gg</small></div></div><div class="card kpi"><div class="ico green">✓</div><div><b>1</b><small>In carico a me</small></div></div><div class="card kpi"><div class="ico purple">⚠</div><div><b>2</b><small>Anomalie tecniche</small></div></div></div><div class="card filters"><div class="chips">${[['all','Tutte'],['mine','Mie attività'],['free','Non assegnate'],['p1','P1'],['late','Scadute'],['rev','Da verificare']].map(x=>`<button class="chip ${chip===x[0]?'on':''}" onclick="chip='${x[0]}';render()">${x[1]}</button>`).join('')}</div><div class="frow"><select class="control"><option>Tutte le società</option>${companies.map(c=>`<option>${c.co}</option>`)}</select><select class="control"><option>Tutte le categorie</option><option>Sanzioni</option><option>Legale</option><option>Registro imprese</option></select><select class="control"><option>Tutti gli assegnatari</option><option>Utente Demo</option><option>Operatore 1</option></select><select class="control"><option>Tutti gli stati</option><option>Nuovo</option><option>In carico</option></select><input class="control" id="q" placeholder="Filtra oggetto o mittente"></div></div>`}${table(data)}`}
function review(){let d=pecs.filter(p=>p.st==='Da verificare'||p.conf<90);return `<div class="toolbar"><div><h2>Revisione classificazioni</h2><span class="mut">Conferma o correggi le proposte automatiche.</span></div></div><div class="grid reviewgrid">${d.map(p=>`<div class="card reviewcard"><div class="row between">${badge(p.p)}<b>Confidenza ${p.conf}%</b></div><h3>${p.co}</h3><b>${p.sub}</b><p class="mut">${p.summary}</p><div class="progress"><span style="width:${p.conf}%"></span></div><div class="actions"><button class="btn sm primary" onclick="confirmReview(${p.id})">Conferma</button><button class="btn sm" onclick="openPec(${p.id})">Modifica</button><button class="btn sm" onclick="toast('Regola demo predisposta')">Crea regola</button></div></div>`).join('')}