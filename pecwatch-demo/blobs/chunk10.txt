<button class=btn onclick="finish(${p.id})">Chiudi</button></div>`;drawer.classList.add('open')}
function closeDrawer(){drawer.classList.remove('open')}function take(id){let p=pecs.find(x=>x.id===id);p.as='Utente Demo';p.st='In carico';toast('Attività presa in carico');openPec(id);render()}function finish(id){let p=pecs.find(x=>x.id===id);p.st='Chiuso';toast('Attività chiusa');closeDrawer();render()}function confirmDue(id){toast('Scadenza confermata con audit');}function confirmReview(id){let p=pecs.find(x=>x.id===id);p.st='Chiuso';toast('Classificazione confermata');render()}
function openTelemaco(cf=''){selectedTm=null;overlay.classList.add('open');modal.innerHTML=`<div class=mhead><b>Acquisisci anagrafica da Telemaco</b><button class=btn onclick="closeModal()">✕</button></div><div class=mbody><div class=box><b>Simulazione</b><br>Nessuna chiamata a InfoCamere viene eseguita. I risultati sono fittizi.</div><div class=row><input id=tmq class=control value="${cf}" placeholder="Denominazione, CF/P.IVA o REA"><button class="btn primary" onclick="searchTm()">Cerca</button></div><div id=tmres></div></div><div class=mfoot><button class=btn onclick="closeModal()">Annulla</button><button class="btn primary" onclick="importTm()">Importa selezionata</button></div>`;if(cf)setTimeout(searchTm,50)}
function searchTm(){let q=tmq.value.toLowerCase();let d=tm.filter(x=>!q||(x.co+x.cf+x.rea).toLowerCase().includes(q));tmres.innerHTML=d.length?d.map((x,i)=>`<div class="result ${selectedTm===i?'on':''}" onclick="selectedTm=${i};searchTm()"><b>${x.co}</b><span class=sub>CF ${x.cf} · ${x.rea} · ${x.st}</span><span class=sub>${x.pec} · ${x.sede}</span></div>`).join(''):'<div class=empty>Nessun risultato demo. Prova “OMEGA” o “SIGMA”.</div>'}
function importTm(){if(selectedTm==null)return toast('Seleziona un risultato');let x=tm[selectedTm];companies.push({co:x.co,cf:x.cf,st:x.st,pec:x.pec,src:'Telemaco API · simulazione'});toast('Anagrafica importata nella demo');closeModal();view='companies';render()}
function openOnboard(){step=1;selectedTm=null;overlay.classList.add('open');drawWizard()}
function drawWizard(){modal.innerHTML=`<div class=mhead><b>Onboarding nuova casella PEC</b><button class=btn onclick="closeModal()">✕</button></div><div class=mbody><div class=steps>${[1,2,3,4].map(i=>`<span class="step ${i<=step?'on':''}"></span>`).join('')}</div>${step===1?`<h3>1. Origine anagrafica</h3><p class=mut>Acquisisci i dati tramite simulazione Telemaco oppure prosegui manualmente.</p><div class=actions><button class="btn primary" onclick="openTelemaco()">Cerca su Telemaco</button><button class=btn onclick="step=2;drawWizard()">Inserimento manuale</button></div>`:step===2?`<h3>2. Società e domicilio digitale</h3><div class=form><div class=field><label>Denominazione *</label><input id=wco class=control value="OMEGA LOGISTICA S.R.L."></div><div class=field><label>CF/P.IVA *</label><input class=control value="00000000004"></div><div class=field><label>PEC